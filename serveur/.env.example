# serveur/.env.example
# ---------------------------------------------------------
# Renomme ce fichier en .env et complète les valeurs.
# ---------------------------------------------------------

# URL de connexion MongoDB (base dédiée au projet)
MONGO_URI=mongodb+srv://<user>:<pass>@<cluster>/<dbName>?retryWrites=true&w=majority

# Clé secrète pour signer les JWT (génère une valeur longue et unique)
JWT_SECRET=change_me_with_a_long_random_string
# Durée de validité des tokens (ex: "7d", "12h", "30m")
JWT_EXPIRES_IN=7d

# Configuration SMTP (recommandé : Gmail + App Password)
MAIL_USER=s.gilbert5988@gmail.com
MAIL_PASS=app_password_à_générer

# (Optionnel) URL publique du front si tu veux générer des liens dans les emails
FRONTEND_URL=http://localhost:5173
